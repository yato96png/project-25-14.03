import React from "react";
import styled from "styled-components";

const HeaderContainer = styled.header`
  background: #007bff;
  color: white;
  padding: 15px;
  text-align: center;
  font-size: 20px;
`;

const Header = () => <HeaderContainer>My Twitter Clone</HeaderContainer>;

export default Header;
